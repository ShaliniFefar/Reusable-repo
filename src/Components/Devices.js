import React from 'react';

const Devices = (props) => {
    return (
        <div>
            <label>Enter Device name to search </label>
            <input type="text" name="search"/>
        </div>
    )
}

export default Devices;